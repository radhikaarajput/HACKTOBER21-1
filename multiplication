#include<iostream.h>
#include <stdio.h>
int main() {
//takes a no as an input and returns its table from 1 to 10
  int n, i;
  printf("Enter an integer: ");
  scanf("%d", &n);
  for (i = 1; i <= 10; ++i) {
    printf("%d * %d = %d \n", n, i, n * i);
  }
  return 0;
}
